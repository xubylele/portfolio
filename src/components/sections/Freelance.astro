---
import { getLangFromUrl } from '../../i18n/utils';
import FreelanceES from './es/Freelance.astro';
import FreelanceEN from './en/Freelance.astro';

const currentLang = getLangFromUrl(Astro.url);
---

{currentLang === 'es' ? <FreelanceES /> : <FreelanceEN />}
