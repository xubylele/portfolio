---
import { getLangFromUrl } from '../../i18n/utils';
import ProjectsES from './es/Projects.astro';
import ProjectsEN from './en/Projects.astro';

const currentLang = getLangFromUrl(Astro.url);
---

{currentLang === 'es' ? <ProjectsES /> : <ProjectsEN />}
