---
import { getLangFromUrl } from '../../i18n/utils';
import ContactES from './es/Contact.astro';
import ContactEN from './en/Contact.astro';

const currentLang = getLangFromUrl(Astro.url);
---

{currentLang === 'es' ? <ContactES /> : <ContactEN />}
