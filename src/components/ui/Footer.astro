---
import { getLangFromUrl } from '../../i18n/utils';
import FooterES from './es/Footer.astro';
import FooterEN from './en/Footer.astro';

const currentLang = getLangFromUrl(Astro.url);
---

{currentLang === 'es' ? <FooterES /> : <FooterEN />}
