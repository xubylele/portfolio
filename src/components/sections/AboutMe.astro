---
import { getLangFromUrl } from '../../i18n/utils';
import AboutMeES from './es/AboutMe.astro';
import AboutMeEN from './en/AboutMe.astro';

const currentLang = getLangFromUrl(Astro.url);
---

{currentLang === 'es' ? <AboutMeES /> : <AboutMeEN />}
