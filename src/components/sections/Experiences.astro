---
import { getLangFromUrl } from '../../i18n/utils';
import ExperiencesES from './es/Experiences.astro';
import ExperiencesEN from './en/Experiences.astro';

const currentLang = getLangFromUrl(Astro.url);
---

{currentLang === 'es' ? <ExperiencesES /> : <ExperiencesEN />}
